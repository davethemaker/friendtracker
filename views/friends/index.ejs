<!DOCTYPE HTML>
<html lang="en">
<head>
  <title>Friend List</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
  
  <h3>Friend List:</h3>
  <a href="/friends">All Friends</a>
  <% if(friends.length === 0){ %>
     <p>Feeling pretty lonely!</p>
  <% } %>
  <a href="/">Home</a>
  <ul>
    <% for(var i = 0; i < friends.length; i++){ %>
    <li> <%= friends[i].name %> </li>
    <% } %>
  </ul>
</body>
</html>